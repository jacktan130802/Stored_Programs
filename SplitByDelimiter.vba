Function splitbyDelimiter(x As String, index As Integer) As String

Dim splitCatcher As Variant
Dim temp As String
Dim counter As Integer
splitCatcher = Split(x, ",")
'For counter = 1 To Len(x)
'    if Split(

temp = splitCatcher(index)

splitbyDelimiter = temp


End Function
